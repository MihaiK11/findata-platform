[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "findata-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.49.0",
    "openai>=1.86.0",
    "fastapi>=0.136.1",
    "httpx>=0.28.1",
    "mcp>=1.6.0",
    "motor>=3.7.1",
    "numpy>=2.2.0",
    "nasdaq-data-link>=1.0.4",
    "pandas>=2.2.3",
    "plotly>=5.24.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "python-dotenv>=1.2.2",
    "streamlit>=1.45.0",
    "uvicorn>=0.46.0",
    "yfinance>=1.4.1",
]

[project.scripts]
findata-mcp = "mcp_server.server:main"

[tool.hatch.build.targets.wheel]
packages = ["app", "streamlit_app", "mcp_server"]
